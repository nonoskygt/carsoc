<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Tema fijo oscuro y sin barra de titulo. El tablero pinta su propia
      paleta en Canvas, asi que el tema solo tiene que quitarse de en medio
      y no provocar un flash blanco al arrancar.
    -->
    <style name="Theme.S2000Dash" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
